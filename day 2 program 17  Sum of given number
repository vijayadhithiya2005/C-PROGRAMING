sum of given number
#include <stdio.h>

int main() {
    int num, sum = 0;

    do {
        scanf("%d", &num);
        if (num != -1) {
            sum += num;
        }
    } while (num != -1);

    printf("%d", sum);

    return 0;
}
