series 1
 #include <stdio.h>
int main() {
    int n, i;
    int value = 6;
    int increment = 3;
    scanf("%d", &n);
    for (i = 1; i <= n; i++) {
        printf("%d ", value);
        value += increment;
        increment += 2;
    }

    return 0;
}
 
  
