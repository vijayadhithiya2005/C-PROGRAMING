product of range
  #include <stdio.h>

int main() {
    int num;
    unsigned long long fact = 1;
    scanf("%d", &num);
    for (int i = 1; i <= num; ++i) {
        fact *= i;
    }
    printf("Product of value is %llu\n", fact);

    return 0;
}

