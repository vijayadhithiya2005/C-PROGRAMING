harry potter
#include <stdio.h>

int main() {
    int num, firstDigit, lastDigit, sum;
    scanf("%d", &num);
    lastDigit = num % 10;
    while (num >= 10) {
        num = num / 10;
    }
    firstDigit = num;
    sum = firstDigit + lastDigit;
    printf("%d\n", sum);

    return 0;
}
